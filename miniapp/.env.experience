# 体验版 / 真机调试环境（无需域名、无需备案）
# 连腾讯云公网服务器 82.156.62.59（开发调试用，HTTP+IP 需开发者工具勾「不校验合法域名」）。
# 若改用局域网调试，把下面的 IP 换成电脑「手机所连 WiFi 对应网卡」的 IPv4：
#   本机 WLAN(无线)   = 192.168.3.4
#   本机 以太网(有线) = 192.168.3.2
# 查看方式：PowerShell 执行 Get-NetIPAddress -AddressFamily IPv4
#
# ⚠️ 后端必须用 0.0.0.0 启动，否则手机访问不到：
#   cd 项目根目录 && PORT=3300 HOST=0.0.0.0 node backend/server.js
#
VITE_API_BASE_URL=http://82.156.62.59:3300/api
